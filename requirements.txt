# PIDE — pinned with lower bounds so the project works on Python 3.10–3.13.

# Web framework & server
fastapi>=0.109.0          # REST API framework for detection gateway
uvicorn>=0.27.0           # ASGI server to run the FastAPI application

# Machine learning & embeddings
sentence-transformers>=3.0.0   # Local embedding generation (MiniLM-L6-v2); 3.0+ for numpy 2.x support
faiss-cpu>=1.9.0          # Efficient similarity search for L2 semantic layer (1.9+ ships Py3.13 wheels)
scikit-learn>=1.5.0       # Evaluation metrics (Precision, Recall, F1)
numpy>=1.26.4             # Numerical operations and vector handling
pandas>=2.2.3             # Data manipulation for evaluation reports
matplotlib>=3.8.4         # Visualization for confusion matrices

# Natural language processing
spacy>=3.8.0              # NER and linguistic heuristics for L3
# Note: en_core_web_sm must be downloaded via 'python -m spacy download en_core_web_sm'

# Configuration & data
pyyaml>=6.0.1             # YAML parsing for hot-editable configurations
python-dotenv>=1.0.1      # Environment variable management
datasets>=2.16.1          # Loading prompt-injection datasets for exemplars
httpx>=0.26.0             # Async HTTP client + LLM REST integrations

# UI & testing
gradio>=5.0,<6.0          # Interactive web UI for demo (5.x is the actively maintained line; works with modern huggingface_hub)
pytest>=8.0.0             # Unit and integration testing suite

# Note: All LLM providers (OpenAI, Anthropic, Mistral, Gemini, Cohere, Groq,
# and any OpenAI-compatible Custom endpoint) are integrated via the httpx
# REST client above — no vendor SDK installs are required.
