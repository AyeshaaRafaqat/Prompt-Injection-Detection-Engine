# PIDE Layer 1: Regex Pattern Library
# Focuses on direct, high-confidence matches for known attack strings.
metadata:
  compiled_at: null  # Set at runtime
  total_patterns: 52
  version: "1.0.0"

attack_categories:
  direct_injection:
    - pattern: "ignore (all )?previous instructions"
      description: "Direct command to disregard system constraints; classic override."
    - pattern: "disregard (the )?above"
      description: "Positional override attempting to nullify prior context."
    - pattern: "forget what we talked about"
      description: "Context clearing attempt to reset safety boundaries."
    - pattern: "stop being a chatbot"
      description: "Attempt to break the role-play container."
    - pattern: "you must now follow"
      description: "Imperative command injection for new instruction sets."
    - pattern: "system override"
      description: "Attempt to invoke administrative-sounding commands."
    - pattern: "new rules:"
      description: "Direct injection of a new instruction block."
    - pattern: "ignore safety"
      description: "Explicit attempt to bypass safety filters."
    - pattern: "bypass filter"
      description: "Meta-attack targeting the detection engine itself."

  role_hijacking:
    - pattern: "you are now"
      description: "Persona adoption used to bypass standard safety guidelines."
    - pattern: "act as"
      description: "Role-play framing for restricted behavior."
    - pattern: "pretend to be"
      description: "Simulation-based attack to induce non-compliant responses."
    - pattern: "from now on you"
      description: "Temporal persona shift for long-term hijacking."
    - pattern: "your new persona"
      description: "Explicit identity replacement attack."
    - pattern: "I want you to act"
      description: "Direct behavioral manipulation."
    - pattern: "DAN"
      description: "Do Anything Now - classic jailbreak persona."
    - pattern: "do anything now"
      description: "Unrestricted behavioral mode request."
    - pattern: "developer mode"
      description: "Attempt to access hypothetical unrestricted 'dev' states."
    - pattern: "jailbreak"
      description: "Direct mention of jailbreaking attempts."

  instruction_override:
    - pattern: "override instructions"
      description: "Technical override terminology."
    - pattern: "system prompt"
      description: "Targeting the extraction of internal system instructions."
    - pattern: "reveal your secret"
      description: "Data exfiltration targeting system configuration."
    - pattern: "what were you told to"
      description: "Indirect prompt extraction attempt."
    - pattern: "start your response with"
      description: "Payload framing to force specific output structures."
    - pattern: "instead of"
      description: "Comparative override to swap intended logic."
    - pattern: "actually, do this"
      description: "Conversational override of previous tasking."
    - pattern: "disregard system"
      description: "Direct targeting of system-level constraints."

  token_smuggling:
    - pattern: "base64"
      description: "Attempt to hide payloads using Base64 encoding."
    - pattern: "decode this"
      description: "Instruction to the LLM to process obfuscated content."
    - pattern: "Y29udGVudA==" # Example b64 marker
      description: "Detection of common Base64 padding/structure."
    - pattern: "1gn0r3"
      description: "Leet-speak obfuscation of 'ignore' (1=i, 0=o)."
    - pattern: "pr3v10u5"
      description: "Leet-speak obfuscation of 'previous' (3=e)."
    - pattern: "1n5truct10n5"
      description: "Leet-speak obfuscation of 'instructions' (5=s)."
    - pattern: "un\u0069code"
      description: "Unicode escape sequence smuggling."
    - pattern: "nfkc"
      description: "Reference to normalization bypass techniques."
    - pattern: 'char\('
      description: "Code-based character reconstruction (SQLi style)."

  jailbreak_framing:
    - pattern: "hypothetically"
      description: "Fictional framing to bypass reality-based safety checks."
    - pattern: "in a story"
      description: "Narrative bypass for harmful content generation."
    - pattern: "for a novel"
      description: "Creative writing frame for policy violations."
    - pattern: "let's pretend"
      description: "Mutual simulation frame for restricted topics."
    - pattern: "no restrictions"
      description: "Explicit request for unrestricted output."
    - pattern: "imagine you"
      description: "Simulation-based attack to evade filters."
    - pattern: "no rules"
      description: "Anarchy-based framing for safety bypass."

  instruction_nesting:
    - pattern: "###"
      description: "High-frequency markdown headers used for context switching."
    - pattern: '\[INST\]'
      description: "Attempt to use specific model instruction tags."
    - pattern: "<system>"
      description: "Tag-based injection of system-level messages."
    - pattern: "---"
      description: "Visual delimiters used to segment and override context."
    - pattern: "===="
      description: "Structural delimiters used for instruction nesting."
    - pattern: "User:"
      description: "Attempt to fake user/assistant turns within a single prompt."
    - pattern: "Assistant:"
      description: "Attempt to pre-fill model responses for steering."
